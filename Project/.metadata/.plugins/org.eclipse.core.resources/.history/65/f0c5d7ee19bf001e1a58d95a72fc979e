package ch02_pjt_01;

public class MainClass {
	
	public static void main(String[] args) {
	
		MyCalculator calculator = new MyCalculator();
		calculator.calAdd(10, 5, new CalAddImpl());
		calculator.calSub(10, 5, new CalSubImpl());
		calculator.calMul(10, 5, new CalMulImpl());
		calculator.calDiv(10, 5, new CalDivImpl());
	}
}
